- helper conform update map if contains? key (update-if :key fn)
- instrumenter ivr.node ns durant les tests node/types
- factoriser les intercepteurs des events d'action des nodes/types
- creer un reg-action qui:
  - injecte store/verbs/acd/services dans les options
  - vire les coeffects (et l'event ? par l'intercepteur re-frame) des args du handler
- nettoyer les tests des events
- callTime
